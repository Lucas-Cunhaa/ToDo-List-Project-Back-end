const path = require('path');
const config = {
    'config' : path.resolve('src', 'config', 'config.js')
};
const models = {
    'models' : path.resolve('src', 'models')
};
const migrations = {
    'migrations' : path.resolve('src', 'migrations')
}
console.log(migrations)
console.log(models)
console.log(config)

module.exports = { config, models, migrations } ;
